# docker-dns-rs integration tests

Docker Compose runs docker-dns-rs next to a set of alpine containers. A watcher drives those through their lifecycle and queries docker-dns-rs after every step.

```
./integration-tests/tests.sh
```
